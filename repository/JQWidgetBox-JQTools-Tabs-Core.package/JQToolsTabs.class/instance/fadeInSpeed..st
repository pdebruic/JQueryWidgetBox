options
fadeInSpeed: aNumber
	"default: 200
	Only available when used together with the fade effect. This property defines how fast (in milliseconds) the opened pane reveals its content."
	self optionAt: 'fadeInSpeed' put: aNumber