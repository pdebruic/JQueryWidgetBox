views-actions
getView
	"Returns the View Object for the current view.

.fullCalendar( 'getView' ) -> View Object

This is useful if you want to get information about the calendar

"

	self action: 'getView'
