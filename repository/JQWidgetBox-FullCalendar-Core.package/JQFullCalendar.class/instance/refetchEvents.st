event-data-actions
refetchEvents

"refetchEvents 1.3

Refetches events from all sources and rerenders them on the screen.

.fullCalendar( 'refetchEvents' )

Prior to version 1.3, the same effect was achieved by the refresh method.

"

self action:'refetchEvents'